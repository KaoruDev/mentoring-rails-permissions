<% @posts.each do |post| %>
  <h2><%= post.title %></h2>
  <p><%= post.body %></p>
  <p>by: <%= post.user.email %></p>

  <% if post.user == current_user %>
    <p><%= link_to "edit post", edit_post_path(post) %></p>
  <% end %>
<% end %>
